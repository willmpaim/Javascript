const salaPython = ['Melissa', 'Helena', 'Rodrigo'];

const salaJavaScript = ['ju','Leo','Raquel'];

const salasUnificadas = salaPython.concat(salaJavaScript);

console.log(salasUnificadas);

/*passa o parametro dentro de concat para adiconar e unificar as arrays , para unificar, 
 realiza uma nova variavel que vai guardar essa unificação.